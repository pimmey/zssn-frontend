export enum TradeItemsSuffixEnum {
  from_items = 'from_items',
  to_items = 'to_items'
}

export enum TradeSurvivorSuffixEnum {
  from_survivor = 'from_survivor',
  to_survivor = 'to_survivor'
}

export enum FromToSuffixEnum {
  from = 'from',
  to = 'to'
}
